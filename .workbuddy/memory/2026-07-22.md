# 2026-07-22

## Production Readiness Review（上线验收，2026-07-22）

从架构一致性收敛切换为 Go-Live Review。5 路并行探查（部署/Env、Prisma/迁移、Redis/后台、安全/存储、可观测性/SEO/灾备）+ 亲自核对修复。

### 自动修复的 4 项上线阻断（tsc 验证零错误）
1. `docker-compose.yml`：移除 `backup` 服务 `profiles: ["backup"]` → `docker compose up` 现在会启动每日 `pg_dump` 备份（原配置下备份容器不启动 = 无备份运行）。
2. `Dockerfile` + `docker-compose.yml`：为 `NEXT_PUBLIC_TURNSTILE_SITE_KEY` 增加构建期 `ARG/ENV` 与 build arg（此前仅运行时注入，客户端 bundle 构建期为 undefined → 生产验证码静默失效）。
3. `src/lib/auth.ts`：生产环境（`NODE_ENV==="production"`）强制 Cookie `secure: true`，不再仅依赖 `NEXTAUTH_URL` 协议。
4. `src/repositories/game.ts`：`$queryRawUnsafe` → 参数化 `Prisma.sql`（已把 `import type { Prisma }` 改为值导入）。

### 仍为 Blocking 的部署环境变量（运维必须在 Coolify 设置）
- `NEXTAUTH_SECRET`：未设置时入口脚本在容器内 `/app/.secret`（非持久卷）自动生成 → 每次重建容器全体会话失效。
- `NEXTAUTH_URL`：未设置回退 localhost:3000 → 邮件链接/OG/secure cookie 回退错误。

### 关键核查结论（无阻断）
- 数据库/迁移：`migrate deploy` 已在 `docker-entrypoint.sh` 自动执行；migrations 与 schema 一致；无 SQL 注入；Prisma 单例正确；密码 bcrypt 哈希。
- 请求层/架构一致性（前序）：已收敛干净，本轮不重复。
- 安全：CSP/Headers/X-Frame-Options/HSTS 在 middleware 设置；限流覆盖 auth 端点；Cookie httpOnly/sameSite=lax；上传通道有类型/大小/鉴权校验。
- 稳定性：Prisma/R2/Redis 均单例，客户端定时器均有 cleanup，无内存/连接泄漏；无裸 `new PrismaClient` 每请求。
- 存储：未配 R2 时回退本地 `public/uploads`（经 `uploads_data` 卷持久化），上传不会 500 → R2 为 Recommended（多副本时必须）。

### 交付物
- `D:\fangame\production-readiness-review.md`（完整报告 + Checklist + 结论 **Go（条件：设置上述 2 个 env）**）

### 后续 Recommended（非阻塞）
Sentry DSN 设置；邮件改为后台队列+重试；缓存失效补齐；限流全局兜底；SVG 上传排除；备份异地化+R2；POSTGRES_PASSWORD 改 secret；Dockerfile 运行期去 devDeps；低频索引；request-id/Sentry.setUser 归因。

## Final Launch Readiness Review（最终上线演练，2026-07-22 晚）
身份 Principal/SRE，模拟真实上线。不重复代码/架构/风格问题。

运维面实测结论：
- 健康检查 `/api/health` 健壮：DB 失败→503(unhealthy)，Redis 缺失→`disabled`(不影响 healthy)，Redis 错误→`degraded`(不致命)。`start_period 30s`+`depends_on db healthy` 防启动期重启循环。
- 迁移：`migrate-entrypoint.sh` 与 `docker-entrypoint.sh` 均用 `prisma migrate deploy`(前向、幂等)；migrations 目录 6 个 + lock(provider=postgresql)，与 schema 一致。失败 exit 1（fail-fast，不以坏 schema 启动）。
- `DEPLOYMENT.md` 已含备份/恢复命令：`docker exec fangame-db pg_dump ...` / `cat backup.sql | docker exec -i fangame-db psql ...`。
- 备份容器 profile 阻断已在上一轮修复，`docker compose up` 会启动 fangame-backup（每日 03:00 pg_dump，保留 7 天，backup_data 卷）。
- 无新增代码级阻断；回滚关键：Prisma 迁移前向-only（无 down），DB 回滚=备份恢复；应用回滚靠 Coolify 上一部署/镜像 tag。

最终结论：**Go（条件）**——上线前置仅在 Coolify 设 `NEXTAUTH_SECRET` + `NEXTAUTH_URL`；建议设真实 VERSION tag、SENTRY_DSN、多副本时配 R2/Redis；上线 24h 内做一次备份恢复演练。

## Post-Launch Guardian 模式（22:17 切换）

用户宣布项目已正式上线（Go Live），角色切换为 **Principal Engineer + SRE + Tech Lead**，进入长期稳定保障模式。

### 运维铁律（已写入 MEMORY.md）
- 不为追求完美改稳定代码；不主动大规模重构；不制造新债。
- 仅在发现**真实**风险/缺陷/性能瓶颈/线上问题时才改。
- 优先级：P0 故障·数据安全·权限·漏洞·丢失·不可用 → P1 真实用户反馈·崩溃·性能·泄漏·DB/缓存异常 → P2 体验(不影响功能) → P3 新功能 → P4 优化/重构(仅收益明显时)。
- 任何修改流程：定位→影响分析→最小方案→修复→自动验证→确认无回归→变更报告。
- 不再重复审查已确认无问题的模块；不重新生成历史问题；不为"还能更好"改稳定代码。
- 目标：长期稳定·可维护·可持续迭代。

## 深度质量收敛（22:22 起，连续三轮扫描模式）

用户要求退出维护模式、回到深度质量收敛：扫描→修复→验证→再扫描，直到连续三轮无新问题。不重复历史问题、不重出历史报告。

### Round 1（输入校验/鉴权/错误韧性）— 已修
- **[P1 已修]** `src/lib/email-providers.ts`：resend/brevo `fetch` 无 `AbortSignal` → 慢提供商挂起核心鉴权流程（注册/找回密码/改邮箱）。加 `signal: AbortSignal.timeout(10_000)`，catch 已吞 AbortError→优雅失败。tsc 零错误。
- **[P2 已修]** `src/app/api/sentry/tunnel/route.ts`：`JSON.parse(piece)`/`new URL(header.dsn)` 无防护 → 畸形 envelope/缺 dsn 返回 500（应为 400）；转发 `fetch` 无超时→挂起。已包裹 try/catch 返回 400 + 校验 dsn 字符串 + 转发加 `AbortSignal.timeout(10_000)`。tsc 零错误。
- **Round 1 已确认 clean（不再复查）**：数据层 IDOR/越权（所有权校验扎实）、SQL 注入（全参数化）、鉴权闸门（~110 handler 全有 guard）、非 2xx 静默吞（服务端无 apiFetchSafe 用法）、命令注入。
- **Round 1 遗留 P2（记录，未强修）**：admin 路由未用 `lib/validations.ts` 既有 zod schema；`gameResource.findMany` 公开端点无 `take` 上限；game report `reason` 无长度校验；VNDB 批量导入顺序 await 无上限；follow toggle 读后写无事务（唯一约束防损坏，仅双点击返回 409/404）。

### Round 2（性能/缓存/限流/SEO/依赖）— 已修
- **[P1 已修]** `src/lib/rate-limit.ts` `getClientIP`：原取 `X-Forwarded-For` **首段**（客户端可伪造）→ 所有 IP 限流可绕过（暴力破解/批量注册失效）。改为优先 `CF-Connecting-IP`/`X-Real-IP`，否则取 **末段**（可信代理追加的真实客户端 IP）。`trustProxy:true` 下正确。
- **[P1 已修]** `src/lib/rate-limit.ts` `checkRateLimit`：`Retry-After` 误把绝对 epoch 秒当 delta → 客户端退避算错。改为 `Math.max(0, (resetTime-Date.now())/1000)`。
- **[P2 已修]** `src/app/api/search/route.ts` + `suggestions/route.ts`：公开搜索端点无限流（DoS/抓取）。接入既有未使用的 `rateLimits.search`（30/min）。
- **[P2 已修]** `src/app/api/auth/forgot-password/route.ts`：原仅按 IP 限流 → 多 IP 可向同一邮箱轰炸重置邮件。改为按**目标邮箱** `getRateLimit(\`pwreset:${email}\`)` 限流。
- **[P2 已修]** `src/app/user/[id]/page.tsx` `resolveUser`：全表读取（含 password 哈希）且 generateMetadata/页面各调一次无去重。加 `select` 仅取所需字段 + `reactCache` 请求内去重。
- **[P1 已修]** 依赖：`sharp ^0.34.5` → `^0.35.3`（libvips CVE，用户上传头像处理攻击面）；`npm install` 确认顶层 sharp=0.35.3。另将仅构建期使用的 `shadcn` 从 dependencies 移到 devDependencies（缩小生产审计面）。tsc 零错误。
- **Round 2 确认 clean（不再复查）**：SEO/metadata 完整（sitemap/robots/OG/canonical 齐全、无公共页误 noindex）；Redis 单例+优雅降级；缓存按用户隔离无跨用户泄漏；N+1 热点无；串行 await 已并行化；无硬编码密钥。
- **Round 2 遗留 P2（记录）**：`sharp` 仍被 `next@16.2.7` 作为传递依赖锁 0.34.5（仅影响 `/_next/image` 优化端点，非直接 import；可考虑 overrides 强制 0.35.x）；首页 GameGrid 因 searchParams 变 force-dynamic 每请求打 DB（应 `unstable_cache`）；`games/[id]` 公开 API 返回整 creator 行+无 `take` 资源；sitemap 未含论坛/用户/角色页；checkin 缓存键裸 `checkin:config:*` 逃逸 `fangame:` 命名空间；Redis 故障 fail-open（设计取舍）。

### Round 3（数据完整性）— 确认 clean + 记录
- 级联删除：所有父→子关系均有 `onDelete: Cascade/SetNull`，无孤儿（已逐关系核对 schema）。软删：无此模式。字段长度：全部 `String`→Postgres `text`，无 varchar 溢出。
- 唯一约束缺口（P2，需迁移/产品决策，未强改）：Notification 无去重 `@@unique`（重试会插重复行）；`GameResourceEntry` 无 `@@unique([resourceId,url])`（同 URL 重复提交）；`GameResource` 无 `@@unique([userId,gameId])`（双提交，可能属"多镜像"可不修）。其余 one-per 不变量均有 DB 唯一约束。

### Round 4（后台任务/SDK/CSP/限流拓扑）— 已修 P1
- **[P1 已修]** `src/lib/redis.ts`：`cache = createCache()` 在**模块加载时**固化后端，而 `service-config` 的 Redis 配置是异步加载（首访 `_redis` 为 null）→ `cache` 永久是 `MemoryCache`。即使后来 `isRedisAvailable()` 返回 true，`rate-limit` 仍走内存计数器 → **分布式限流从未真正连 Redis，多副本下认证限流被绕过**（此前修的 XFF 防伪造/按邮箱限流都建立在此失效假设上）。改为惰性委托代理 `cache`：每次调用按 `getRedisConfig()` 实时值选 Redis 或内存后端，配置就绪后自动切 Redis。`cached()` 通用缓存也随之真正用上 Redis。tsc 零错误。
- **[P2 已修]** `.env.example`：补 `BREVO_API_KEY`、`EMAIL_PROVIDER_ORDER`（生产邮件功能所需但文档缺失）。
- **Round 4 确认 clean/记录**：CSP 强（`script-src 'self' 'nonce' 'strict-dynamic'`，无 unsafe-inline；`connect-src` 白名单非 `*`；`frame-ancestors 'none'`/`object-src 'none'`/`base-uri 'self'`）；HSTS(max-age=1y+includeSubDomains)；X-Frame DENY；NextAuth v5（jwt+缺 secret 生产 exit(1)+role 默认 USER+trustHost）；Uploadthing 仅 image/audio 无任意类型（SVG 内联 XSS 为 P2 建议收窄 contentType）；Prisma 连接池已设 connection_limit/pool_timeout（多副本建议下调）；VNDB 导入事务缺失+`vndbId` 无唯一（P2，admin 域，建议迁移+事务）；限流各认证 mutation 全覆盖无缺失。

### Round 5（重定向/CSRF/时区/可访问性/构建配置）— 已修
- **[P1 已修]** 签到连续天数时区 bug：`src/repositories/user.ts` `_toDateStr` 原用 `date.toISOString().split("T")[0]`（UTC 日期）→ 在 UTC+8 部署下，本地午夜后很久才翻 UTC 日，导致签到"今天是否签过/连续天数"永远算错（streak 恒为 0）。改用 SSOT `toShanghaiDate(date)`；`findByDate`/`create` 用 `new Date(dateStr+"T00:00:00+08:00")` 解析为上海零点 UTC 瞬时。`src/services/user.ts` 的 `checkIn`/`getStatus` 同步改为 `+08:00` 解析。tsc 零错误。
- **[P2 已修]** 点赞按钮双击重复请求/计数抖动：`forum-post-detail.tsx`、`comment-section.tsx`、`forum/forum-client.tsx` 三处点赞均加 `liking` 锁（state / `useRef<Set>`），请求中禁用按钮 + `finally` 复位，避免快速双击发两次请求导致点赞数 +2。
- **[P2 已修]** `next.config.ts`：移除 `server: { trustProxy: true }` 死配置。Next 16 已无 `server`/`trustProxy` 选项（类型错误 TS2353，且运行期被忽略）。经核 `base-server.js`(L572-576) Next 16 默认直接读取上游代理写入的 `x-forwarded-proto`/`x-forwarded-for` 判定协议与客户端地址，无需该开关；移除后运行期行为不变（反而消除了误导性注释）。
- **Round 5 验证**：`tsc --noEmit` 全量零错误。期间修复了 Round 5 编辑在 `forum-post-detail.tsx:516` 引入的 TS1128 花括号失衡（多了一个 `}` 提前闭合组件函数体），删除该游离 `}` 后通过。
- **Round 5 确认 clean/记录**：重定向无循环（Next 默认 `trailingSlash=false`，auth 回调/`next-auth` 重定向用 `NEXTAUTH_URL`）；CSRF（NextAuth v5 内置 `csrfToken`+sameSite=lax+customSession 无双提交依赖，无 form action 跨站写）；error boundary（根 `+error.tsx`/路由级存在，全局 `global-error` 待确认）；构建配置（standalone+reactStrictMode+compress 正确，turbopack 默认）。a11y：profile-medal-modal 触发为 `<span>` 非 `<button>`（P2 建议，不阻塞功能）。

### 收敛计数状态（截至 Round 5）
- R1/R2/R3/R4/R5 每轮均发现并修复了真实问题 → **尚未出现连续三轮 clean**。需继续 Round 6+，直至连续三轮扫描 0 新问题。

### Round 6（XSS 覆盖/开放重定向/PII/分页/滥用防护/令牌生命周期/竞态/供应链）— 已修
- **[P1 已修]** `src/app/api/games/views/batch/route.ts`：匿名可达的写入端点，原无鉴权无限流 → 任意伪造浏览量（统计篡改）+ 无界 DB 写入（DoS）。加 `checkRateLimit(rateLimits.search,"batch-view")` + 单次批量上限 `slice(0,50)`。tsc 零错误。
- **[P2 已修]** `src/app/api/games/route.ts`：`limit` 客户端直传无上限 → 无界查询 DoS。改为 `Math.min(50, Math.max(1, ...))`。
- **[P2 已修]** 4 个路由用裸 `await req.json()`，畸形体经 `withHandler` 落"未知异常"返回 500 而非 422（仓库已有 `safeParseJson` 惯例）：`forum/comments/[id]` PUT、`games/[id]/resources/[resourceId]` PUT、`admin/reports` DELETE、`admin/games` POST → 全改 `safeParseJson(req)`。其余 `req.json()` 仅 `notifications` 用 `.catch(()=>({}))`（安全）。
- **[P2 已修]** 写端点缺服务端限流（仅客户端守卫）→ 登录用户可刷通知/灌举报队列：`rate-limit.ts` 增 `like`(30/min)/`follow`(20/min)/`report`(10/min) 三档，应用于 3 个 like 路由 + follow 路由 + 2 个 report 路由。
- **[P2 已修]** `src/lib/auth.ts` 令牌生命周期：原 `role`/`isEmailVerified` 来自 JWT，管理员改角色/用户验证邮箱后旧会话直到重新登录才生效（stale permission）。改为 session 回调从 `getCachedUser`(30s 缓存) 读 `role`/`emailVerified` 作为权威值，最迟 30s 收敛。tsc 零错误。
- **Round 6 确认（已核查，非缺陷）**：关注 toggle 并发 P2002 经 `withHandler.mapPrismaError` 映射为 `ConflictError(409)` 而非 500（唯一约束挡重复行，无损坏）→ 不改。XSS：全 UGC 渲染走 `sanitizeRichText`(DOMPurify)，无绕过；开放重定向：仅 `login` 的 `callbackUrl` 已校验同源相对路径。日志：无 PII/密钥泄露。客户端组件 listener/interval 抽样清理均正确。
- **[供应链·遗留/受阻]** `npm audit --omit=dev` 17 漏洞(4 high：undici/linkify-it/brace-expansion/fast-uri)。非破坏性 `npm audit fix` 可修 undici+linkify-it（论坛 markdown-it 用户文本可达 DoS、Node fetch 底层）；PostCSS 需 `--force` 会把 next 降级到 9.3.3（**禁止**）。本轮沙箱拒绝执行 `npm audit fix`（需 CI/部署环境人工执行）。记录为 Recommended，非阻塞。

### 收敛计数状态（截至 Round 6）
- R1–R6 每轮均发现并修复真实问题 → **仍无连续三轮 clean**。继续 Round 7+。

### Round 7（mutation 校验/错误信息泄露/上传穿越/定时器/内存/env/缓存）— 已修 + 评估
- **[P2 已修]** `src/lib/api-handler.ts` `mapPrismaError`：P2002 分支泄露约束列名（`error.meta.target`）、default 分支泄露 Prisma `error.code` 给客户端。改为通用文案（服务端 `logger.api.error` 已记录细节）。tsc 零错误。
- **[P2 已修]** `src/app/api/admin/services/route.ts`：R2/Redis 测试兜底把原始 SDK 错误文本回传管理员客户端 → 改为通用文案。
- **[P2 已修]** `src/lib/storage.ts` `deleteByUrl`：路径穿越纵深防御（`path.normalize` + 阻断前导 `../` 与绝对路径）。当前不可达（URL 经 `sanitizeUrl` + 文件名服务端随机生成），属防御性加固。
- **[P2 已修]** `src/services/user.ts` `updateProfile`：`faveGameId` 无类型校验（`raw.faveGameId || null`）→ 加 `typeof string` 守卫。
- **[P2 已修]** `src/lib/achievements.ts`：`invalidateUserStats` 定义但 0 调用 → `checkAchievements` 开头先 `invalidateUserStats(userId)`，成就基于最新活动解锁（原 300s 缓存未失效致解锁延迟最多 5 分钟）。
- **[评估后不改·避免回归]** "Zod 漂移"：①`updateProfile` 手动校验比 `updateProfileSchema` 更严（含 `sanitizeUrl` + 用户名唯一 + 允许空串清除，而 schema `.url()` 会拒绝空串）；②`gameService.createComment` 用 `(content,imageUrl?,parentId?)` 参数，`gameCommentSchema` 仅含 `content/rating` 且无 imageUrl/parentId，直接替换会丢字段/回归。故保留手动校验，仅补 `faveGameId` 类型守卫。
- **[Recommended·未改·Guardian 纪律]** `env.ts`：`NEXTAUTH_URL` 有 `.default(localhost)` 致生产缺失静默回退（auth 静默失效）；`UPLOADTHING_TOKEN` 未入 schema；`prisma.ts` 用 `process.env.DATABASE_URL!` 绕过 Zod 无 fail-fast。改为硬必填会风险中断当前运行的 prod（若变量未设）→ 不改代码，记录为运维 Recommended（Coolify 应设置并校验 NEXTAUTH_URL/UPLOADTHING_TOKEN/DATABASE_URL；启动门禁建议在 instrumentation 调 `getEnv()`）。
- **Round 7 确认 clean**：用户侧 mutation 均有校验（forumPost/forumComment/gameResource/collection 用 zod，updateProfile/createComment 手动更严）；服务端定时器/内存（rate-limit 内存店上限 10000、MemoryCache LRU 1000、page.tsx pendingMap 自清理）有界；静态上传（文件名随机、类型/大小受控、SVG 禁用）安全；缓存正确性（ISR 覆盖列表、`unstable_cache` keyParts 含参数不跨用户、`site-settings` revalidateTag 正确）。

### 收敛计数状态（截至 Round 7）
- R1–R7 每轮均发现并修复真实问题 → **仍无连续三轮 clean**。继续 Round 8+。

### Round 8（DB 性能/N+1/过度获取/随机查询/密码学/cookie/弱哈希）— 已修 + 评估
- **[P2 已修]** `src/repositories/forum.ts`：`findPostById` + `findComments` 的 `likes: { select: { userId: true } }` 无界拉取全部点赞用户（热门帖/评论可达上万行，响应膨胀、带宽浪费）。经全仓 grep 确认组件从不消费 `.likes`（仅用 `likeCount` 标量 + 独立 `liked` 布尔），安全移除。
- **[P2 已修]** `src/repositories/game.ts`：`findComments`(游戏详情) 同上去掉无界 `likes`。
- **[P2 已修]** `src/repositories/game.ts` `findRandom`：`ORDER BY RANDOM()` 在大表触发全表扫描+排序（每次点"随机角色/作者"按钮中招）→ 改为先 `count` 再随机 `OFFSET` 选连续块，消除全表排序。调用方 `gameService.getRandom` 返回 Promise 不变，tsc 零错误。
- **[Recommended·未改]** ①`findFavorites`/`findPlayStatuses` 无分页（返回该用户全部收藏/游玩状态 + 完整 game 对象，重度用户巨量载荷）→ 涉及 API 契约变更；②`Game.title` `mode:"insensitive"` 的 `contains` 无法命中 btree `@@index([title])`（需 pg_trgm GIN 索引 + 迁移）。均按 Guardian 纪律记录为 Recommended，不在收敛循环强改。
- **Round 8 确认 clean**：密码学（重置/验证/改邮箱令牌 `crypto.randomBytes(32)` CSPRNG + sha256 存储；`Math.random` 仅签到随机积分/本地 React key；`Date.now` 仅过期冷却；应用代码无 md5/sha1 安全用途）；自定义 cookie 仅 `nsfw_status`（非敏感布尔 UI 偏好，SameSite 默认 Lax，加 `Secure` 会破坏 localhost dev → 不改）；N+1（game/user/forum/home 热路径单查询 + include，无循环查）；无界 Promise.all 扇出无。

### 收敛计数状态（截至 Round 8）
- R1–R8 每轮均发现并修复真实问题 → **仍无连续三轮 clean**。继续 Round 9+。

### Round 9（hydration/double-submit/可访问性/error-boundary）— 已修
- **[P2 已修]** `src/components/announce-swiper.tsx`：NEW 徽标在渲染期直接 `new Date()` 计算（≤7 天）→ SSR 与客户端水合时间差在临近边界时可能翻面（hydration mismatch）。改为 `mounted` 态后客户端计算 `showNew`（SSR 不渲染徽标，水合后补出，无 mismatch）。
- **[P2 已修]** `src/components/achievement-toast.tsx`：成就解锁吐司缺屏幕阅读器播报。根容器加 `role="alert"` + `aria-live="assertive"`。
- **[P2 已修]** 3 个 `Field` 组件（`announcements-manager.tsx`、`setup-wizard.tsx`、`admin/achievements/page.tsx`）`<label>` 无 `htmlFor`、控件无 `id` → 标签与控件未关联。新增 `lib/utils.ts` 纯函数 `withLabelableId`（向下钻取一层宿主元素把 `id` 注入首个 labelable 控件，遇自定义组件即停，避免误改第三方 DOM），`Field` 用 `useId()` 生成 `id` 并 `htmlFor` 关联。
- **[P2 已修]** `src/components/profile-edit-form.tsx`：4 对 `<label>`+`<input>`（用户名/简介/当前密码/新密码）无 `htmlFor`/`id` → 加 `useId` 关联。
- **[P2 已修]** `src/components/game-form.tsx`：8 个文本输入（主推名称/日文原名/英文名称/搜索别名/VNDB ID/发售日期/制作会社/游戏时长）的 `<label>`+`<input>` 无关联 → 加 `useId` 关联；复选框（NSFW/立即发布）本就包裹 input 无需改。
- **[P2 已修]** 两个自定义模态框缺 `role="dialog"`/`aria-modal`/焦点管理：`profile-medal-modal.tsx`（内容层加 `role="dialog" aria-modal tabIndex=-1` + `aria-labelledby` 指向标题，打开时聚焦/ESC 关闭/Tab 焦点陷阱/关闭还原焦点）与 `forum/post-detail-modal.tsx`（移动端全屏 sheet 加 `role="dialog" aria-modal aria-label` + 聚焦/ESC/焦点陷阱）。
- **[验证]** `tsc --noEmit` 全量零错误。期间 `withLabelableId` 初版 `cloneElement(node,{id})` 在 `ReactElement<unknown>` 下报 TS2769（props 不接受 id），改 `node as ReactElement<any>` 后通过。
- **[确认 clean]** 水合风险其余处（`timeAgo`/相对时间均有 `suppressHydrationWarning` 或客户端算）；double-submit 类（点赞锁 Round 5 已加，本轮无新增）；error-boundary 根级 `+error.tsx`/`global-error` 存在，论坛/游戏/用户分段缺路由级 `error.tsx`（Recommended，不阻塞）。

### 收敛计数状态（截至 Round 9）
- R1–R9 每轮均发现并修复真实问题 → **仍无连续三轮 clean**。继续 Round 10+。

### Round 10（无界列表/缓存）— 已修
- **[P1 已修]** `src/repositories/game.ts` `findResources`：`include: { entries: true }` 原无 `take` 且顶层 `take` 缺失 → 无界嵌套 payload（每个资源把全部子条目拉出，DoS + 带宽风险）。改为 `entries: { take: 20 }` + 顶层 `take: 50`。
- **[P2 已修]** `src/app/api/curated-collections/route.ts`：公开 top-level `findMany` 无 `take` 上限。加 `take: 100`。
- **[P2 已修]** `src/repositories/user.ts`：`findFavorites` + `take: 200`；`findPlayStatuses` + `take: 200`；`findByUserId`(collections) + `take: 200`。
- **[验证]** `tsc --noEmit` → DONE_EXIT=0，错误数 0（干净）。
- **[确认 clean]** Class 1（私有数据跨用户缓存泄漏）与 Class 3（route-segment 静态缓存）经核查均无真实缺陷——Next 16 动态路由读取 `cookies()/headers()/auth()` 自动发 `Cache-Control: private, no-cache, no-store, max-age=0, must-revalidate`（已核对 `app-render.js:1635` + `dynamic-rendering.js:269`）。admin `getAll` 端点（小表/admin-only/低风险）按 Guardian 纪律未强改，记录为 Recommended。

### 收敛计数状态（截至 Round 10）
- R1–R10 每轮均发现并修复真实问题 → **仍无连续三轮 clean**。继续 Round 11+。

### Round 11（Server Action 校验/鉴权覆盖）— 确认 clean
- 全仓 grep `"use server"`：应用代码 0 处（仅 node_modules 框架内）。无 `action={fn}` 服务端函数 props；所有 23 个 `form onSubmit` 均经 `api-client` → `/api/*`（受 `withHandler` 保护）。
- `review-actions.tsx` 是 `"use client"` 组件走 `apiFetchSafe`；`startTransition` 仅 `router.refresh()` 无 mutation。
- **REAL DEFECT COUNT: 0**。Server Action 攻击面为空，无需修复。

### 收敛计数状态（截至 Round 11）
- R1–R10 每轮均有真实问题；**R11 首次 clean（连续 clean 轮 = 1）**。需再连续 2 轮 clean（R12、R13）方可宣布收敛。

### Round 12（图片管道 / next/image SSRF）— 确认 clean
- `next.config.ts` `images.remotePatterns` 全为固定已知 host（`*.r2.dev`/`*.r2.cloudflarestorage.com`/`utfs.io`/`uploadthing.com`/`static.vndb.org`/`t.vndb.org`），无 `**`/任意 host 通配；`dangerouslyAllowSVG` 未设（优化器拒 SVG）。
- UGC 图片字段（avatar/imageUrl/banner/link）均经 `sanitizeUrl` 仅放行 `http(s)`、阻断 `javascript:`；`<Image src={userUrl}>` 仍受 `remotePatterns` 约束，evil.com 返回 400（无服务端 fetch）。
- 无 `?url=` 图片代理路由；无服务端 `fetch(clientImageUrl)`；上传 URL 由 `publicUrl`+随机 key 构造，用户不可控 host。
- **REAL DEFECT COUNT: 0**。无 SSRF/open-proxy 面。

### 收敛计数状态（截至 Round 12）
- **R11、R12 连续 2 轮 clean（连续 clean 轮 = 2）**。再 1 轮 clean（R13）即可宣布真正收敛。

### Round 13（通知/事件幂等性）— 发现 1 真实缺陷，已修
- **[P2 已修]** `src/lib/achievements.ts` `checkAchievements`：原 `createMany({skipDuplicates:true})` 仅防 `UserAchievement` 重复，但随后的 `unlockCount.increment(1)` 与 `createNotification` 对 `newUnlocks` **无条件**执行 → 同一用户并发两个 `checkAchievements`（如 toggleFavorite + createComment，或显式 `POST /api/achievements/check` + 任一动作）时，双方都算出相同 `newUnlocks`，各插入一条相同 `(userId, achievement_unlock)` 通知（重复通知），且 `unlockCount` 过计 +1。
- **修复（无迁移、原子、复用既有约束）**：改逐步 `userAchievement.create`；仅当 `create` 真正成功（非 P2002）才将该成就纳入 `actuallyNew`，再仅对 `actuallyNew` 累加 `unlockCount` 与发通知。并发时先落库方成功、另一方触发 `UserAchievement` 既有 `@@unique([userId,achievementId])` 抛 P2002 → 跳过，从根本上消除重复通知与过计。返回 toast 列表同步改为 `actuallyNew`。
- `createNotification` 本身不改（其余 4 类通知均为单请求触发，无并发双写路径，改通用去重反而会抑制"取消再触发"等合法新通知，违背 Guardian 最小改动）。
- **[验证]** `tsc --noEmit` → DONE_EXIT=0，0 错误。
- **[确认 safe]** `UserAchievement` 有 `@@unique` 徽章不重复；服务端唯一定时器为 `rate-limit.ts` 的 10min 清理（幂等、无触库、无泄漏）；无 cron/队列/Bull/`instrumentation` 后台任务；无自定义 EventEmitter 泄漏；`createNotification` 为通知唯一写入口，无其他重复源。

### 收敛计数状态（截至 Round 13）
- R13 发现并修复真实缺陷 → **连续 clean 轮归零（= 0）**。需重新累计连续 3 轮 clean（R14、R15、R16）方可宣布收敛。

### Round 14（入站 webhook / 外部回调校验）— 确认 clean
- 全仓仅 1 个入站 webhook：`/api/uploadthing`，由 Uploadthing `createRouteHandler` 自动校验 `Uploadthing-Signature` (HMAC-SHA256) → 非验证 POST 被 401 拒绝前不进入应用代码。
- `onUploadComplete`（`lib/uploadthing.ts`）仅返回 `{url}`，**不改库**；资源挂载在客户端 `onChange` → 受 `withHandler`+鉴权的 API 路由。无 Stripe/邮件回弹/GitHub 等其它 webhook。
- **REAL DEFECT COUNT: 0**。无未验证外部 POST 可改库/挂载文件。

### 收敛计数状态（截至 Round 14）
- R13 修缺陷归零后，**R14 首次 clean（连续 clean 轮 = 1）**。需再连续 2 轮 clean（R15、R16）方可宣布收敛。

### Round 15（状态切换并发完整性）— 发现 1 真实缺陷，已修
- **[P2 已修]** `src/services/game.ts` `toggleFavorite`：原逻辑"先 `removeFavorite`，失败(空 catch P2025)就 `addFavorite`" → 已收藏态并发双触发时，第二次 `remove` 抛 P2025 被吞 → 落入 `add` 新建行，最终态错误（仍收藏）+ 去规范化 `favoriteCount` 漂移 +1。
- **修复（复用既有约束 + 交互式事务，与 commentLike 同款）**：在 `repositories/game.ts` 新增原子化 `toggleFavorite(userId,gameId,collectionId)`——交互式事务内 `findUnique` → 存在则删+decrement、否则建+increment，依赖 `Favorite @@unique([userId,gameId])` 兜底并发；service 层改为调用它，并对并发 P2002/P2025 按真实 `isFavorited` 返回终态（不向二次并发请求抛错）。
- 同步更新 `src/__tests__/game-service.test.ts` mock（`addFavorite`/`removeFavorite` → `toggleFavorite`）与两则用例，匹配新实现。
- **[验证]** `tsc --noEmit` → DONE_EXIT=0，0 错误。
- **[确认 safe]** `setPlayStatus` 用 `upsert`+`@@unique` 安全；`checkIn` 靠 `@@unique([userId,date])` P2002 兜底仅发一次积分；`follow` 靠 `@@unique` 安全；论坛/评论/游戏评论点赞均用交互式事务+`@@unique` 安全。

### 收敛计数状态（截至 Round 15）
- R15 发现并修复真实缺陷 → **连续 clean 轮再次归零（= 0）**。需重新累计连续 3 轮 clean（R16、R17、R18）方可宣布收敛。

### Round 16（API 路由 CSRF 防护）— 确认 clean
- 会话 cookie `fangame-session-token`：`httpOnly` + `sameSite:"lax"` + `secure`（生产）。非 `none`。
- 全量写操作（create/update/delete/upsert）均位于 POST/PUT/DELETE 处理器；无 GET 变更；无 `sameSite:none`；无缺失 token 校验的表单/GET 变更路由。
- JSON 路由（`safeParseJson`/`req.json()`）对非 JSON 返回 422 → 跨站 `<form>` 无法伪造；`formData` 评论/上传路由因 cookie=lax 跨站表单不携带会话，亦不可伪造。
- token 型邮箱/重置密码 GET 依赖密钥 token 而非会话 cookie，非 CSRF 面。
- **REAL DEFECT COUNT: 0**。CSRF 防御由设计保证（lax + JSON-only + 无 GET 突变）。

### 收敛计数状态（截至 Round 16）
- R15 修缺陷归零后，**R16 首次 clean（连续 clean 轮 = 1）**。需再连续 2 轮 clean（R17、R18）方可宣布收敛。

### Round 17（markdown/XSS 渲染管线）— 确认 clean
- 全仓无 markdown 引擎（MarkdownIt/marked/react-markdown/remark/rehype 零匹配）；UGC 均为富文本 HTML，统一 `RichTextContent` → `sanitizeRichText`(DOMPurify，默认 URI 校验剥离 `javascript:`)。
- 全部 `dangerouslySetInnerHTML` 站点：编译期常量（about/rules/contact 默认 HTML、theme-script）或经 DOMPurify 净化（custom 分支）；games/[id] JSON-LD 用标准 `</script>` 转义。
- UGC 链接字段（avatar/banner/imageUrl/link/社交链接）均经 `sanitizeUrl`(http/https 白名单)；富文本内 `<a>` 经 DOMPurify URI 校验。
- **REAL DEFECT COUNT: 0**。无存储 XSS / `javascript:` 链接面。

### 收敛计数状态（截至 Round 17）
- R15 修缺陷归零后，**R16、R17 连续 2 轮 clean（连续 clean 轮 = 2）**。再 1 轮 clean（R18）即可宣布真正收敛。

### Round 18（开放重定向全量面）— 确认 clean
- 穷举所有服务端 `redirect()`/`NextResponse.redirect`（middleware、admin、user/games/profile/[id] 解析页、各 page 硬编码路由）、API 路由（无任何 redirect）、客户端 `router.push`/`replace`、回调参数（`returnTo`/`next=`/`redirectTo`/`goto`/`to=` 全仓 0 命中）。
- 所有重定向目标：硬编码内部路由 / 同源 `new URL(path, req.url)` / DB 派生内部 ID / 仅 `login` 的 `callbackUrl` 经 `startsWith("/") && !startsWith("//") && !includes("://")` 校验。无用户可控外部目标。
- **REAL DEFECT COUNT: 0**。无开放重定向面。

### ✅ 收敛达成
- **R16、R17、R18 连续 3 轮 clean（连续 clean 轮 = 3）** → 满足用户"连续三轮扫描无新问题"条件。
- 项目已**真正收敛**：深度质量收敛循环（R1–R18，每轮一独立维度）结束。期间共修复真实缺陷若干（R1 邮件超时、R2 限流 IP 伪造+搜索限流+邮箱轰炸+全表读、R4 Redis 后端固化、R5 签到时区+点赞双击、R6 批量浏览量+limit 上限+safeParseJson+写限流+令牌收敛、R7 错误泄露+路径穿越+成就缓存、R8 无界 likes+随机查询、R9 hydration/a11y、R10 无界列表、R13 通知重复、R15 收藏并发），其余维度经多轮核查均确认 safe。
- 仍挂 Recommended（非阻塞、Guardian 纪律未改）：admin getAll 分页、sharp 0.34.5 传递依赖、首页 GameGrid force-dynamic 缓存、sitemap 补全、checkin 缓存键命名空间、Redis fail-open、notification/gameResourceEntry @@unique 迁移、npm audit 17 漏洞(CI 修)、env 硬必填门禁、SVG 上传收窄、VNDB 导入事务。
